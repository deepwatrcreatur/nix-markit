name: Update Package

on:
  workflow_dispatch:
  schedule:
    - cron: "23 6 * * 1"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@main

      - uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Update package
        run: nix shell nixpkgs#curl nixpkgs#jq nixpkgs#nix-update nixpkgs#nodejs_24 -c ./scripts/update-package

      - name: Build package
        run: nix build .#markit

      - name: Create pull request
        uses: peter-evans/create-pull-request@v7
        with:
          branch: automation/update-package
          commit-message: "chore: update markit package"
          title: "chore: update markit package"
          body: |
            Automated package update generated by `scripts/update-package`.
